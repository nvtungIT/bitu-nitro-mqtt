///
/// HybridMQTTSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2024 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif



#include <string>

namespace margelo::nitro::math {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `MQTT`
   * Inherit this class to create instances of `HybridMQTTSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridMQTT: public HybridMQTTSpec {
   * public:
   *   HybridMQTT(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridMQTTSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridMQTTSpec(): HybridObject(TAG) { }

      // Destructor
      virtual ~HybridMQTTSpec() { }

    public:
      // Properties
      

    public:
      // Methods
      virtual void connect(const std::string& clientID, const std::string& host, double port) = 0;
      virtual void publish(const std::string& topic, const std::string& message) = 0;
      virtual void subscribe(const std::string& topic) = 0;
      virtual void disconnect() = 0;
      virtual bool isConnected() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "MQTT";
  };

} // namespace margelo::nitro::math
