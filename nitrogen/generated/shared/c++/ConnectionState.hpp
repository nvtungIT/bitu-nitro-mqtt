///
/// ConnectionState.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2024 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::mqtt {

  /**
   * An enum which can be represented as a JavaScript union (ConnectionState).
   */
  enum class ConnectionState {
    CONNECTED      SWIFT_NAME(connected) = 0,
    DISCONNECTED      SWIFT_NAME(disconnected) = 1,
    CONNECTING      SWIFT_NAME(connecting) = 2,
  } CLOSED_ENUM;

} // namespace margelo::nitro::mqtt

namespace margelo::nitro {

  using namespace margelo::nitro::mqtt;

  // C++ ConnectionState <> JS ConnectionState (union)
  template <>
  struct JSIConverter<ConnectionState> {
    static inline ConnectionState fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("connected"): return ConnectionState::CONNECTED;
        case hashString("disconnected"): return ConnectionState::DISCONNECTED;
        case hashString("connecting"): return ConnectionState::CONNECTING;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum ConnectionState - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, ConnectionState arg) {
      switch (arg) {
        case ConnectionState::CONNECTED: return JSIConverter<std::string>::toJSI(runtime, "connected");
        case ConnectionState::DISCONNECTED: return JSIConverter<std::string>::toJSI(runtime, "disconnected");
        case ConnectionState::CONNECTING: return JSIConverter<std::string>::toJSI(runtime, "connecting");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert ConnectionState to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("connected"):
        case hashString("disconnected"):
        case hashString("connecting"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
